Role: You are a Senior Python Automation Engineer. Your task is to build a robust, production-grade music generation bot for musichero.ai.

Project Structure:
I am providing you with the Captured API Methods and my hCaptcha Bypasser logic. You need to analyze both and merge them into a single, clean main.py file that supports "Unlimited" generation.

1. The Captured Logic (Reverse Engineered):

Create Endpoint: POST https://api.musichero.ai/api/v1/suno/create

Polling Endpoint: GET https://api.musichero.ai/api/v1/suno/pageRecordList?pageNum=1

Site Key: 6520ce9c-a8b2-4cbe-b698-687e90448dec

Required Headers:

uniqueId: Must be a fresh uuid.uuid4().hex for every single request cycle.

verify: This must contain the hCaptcha Token returned by the bypasser.

User-Agent: A modern Chrome/Windows string.

2. The Captcha Integration (Analyze & Implement):
I am using a hCaptcha solver. Please create a function get_hcaptcha_token() that uses the Site Key provided. (I will paste my specific provider's API key in Replit Secrets). The script must:

Call the solver before every POST /create request.

If the solver fails, retry up to 3 times.

3. Execution Workflow (Clean Code Architecture):

Session Management: Use requests.Session() to handle JSESSIONID cookies automatically.

Modes Support: The script should handle two modes:

Simple: Only requires a prompt.

Custom: Requires prompt (as style), lyrics, and title. (Set customMode: true).

Polling Logic: Once the song is created, the script should hit the pageRecordList every 10 seconds.

Data Extraction: From the JSON response, navigate to data -> records[0]. If status == "finished", extract and print the audioUrl.

4. Anti-Bot & Stability Features:

Implement Random Delays (time.sleep) between steps (2-5 seconds).

Add Error Handling for:

429 Too Many Requests: (Wait and retry with a new uniqueId).

403 Forbidden: (Check if the captcha token expired).

Timeout: If the song isn't ready in 3 minutes, skip to the next.

Final Output Format: Provide a clean, modular Python script with comments explaining each step. Ensure all sensitive keys are pulled from os.getenv().