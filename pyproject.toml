[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "curl-cffi>=0.14.0",
    "httpx>=0.28.1",
    "playwright>=1.58.0",
    "requests>=2.32.5",
]
